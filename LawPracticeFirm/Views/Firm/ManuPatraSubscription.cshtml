@{
    Layout = "~/Views/Shared/_ManuPanelPartial.cshtml";
}
@if (ViewBag.checkuser == "0")
{
    <div style="margin-top:13%;text-align:center;font-weight:bold"><center>You are not authorized to access this page.</center></div>
}
else
{
    <i class="pp-icon-user"></i>
    <link href="~/panelDesign/vendor/datatables-plugins/dataTables.bootstrap.css" rel="stylesheet">
    <script src="/CssJsfiles/js/bootstrap3-typeahead.min.js"></script>
    <!-- DataTables Responsive CSS -->
    <link href="~/panelDesign/vendor/datatables-responsive/dataTables.responsive.css" rel="stylesheet">
    <script src="~/Manupanel/pnotify/pnotify.custom.min.js"></script>
    <link href="~/Manupanel/pnotify/pnotify.custom.min.css" rel="stylesheet" />
    <link href="~/Manupanel/validation/screen.css" rel="stylesheet" />
    <script src="~/Manupanel/validation/jquery.validate.js"></script>
    <link href="~/Manupanel/css/manudocument.css" rel="stylesheet" />
    @*<script type="text/javascript" src="~/Scripts/Firm/Pages/ManupatraSubscription.js"></script>*@
    <style>
        .select_court {
            width: 128px !important;
        }

        table {
            background: transparent;
        }

            table th {
                background: #5c5c5c;
            }

            table.sortable thead tr th {
                background-repeat: no-repeat;
                background-position: center right;
                cursor: pointer;
                background-image: none !important;
            }

        tr:nth-child(even) {
            background-color: #dddddd;
        }

        table p {
            margin: 0;
            padding: 0;
            font-size: 13px;
            font-weight: normal;
            cursor: pointer;
        }

        .modal-dialog {
            width: 93%;
            margin: 30px auto;
        }

        .modal-body {
            position: relative;
            padding: 10px;
        }

        .modal-footer {
            padding: 6px 20px 7px;
            margin-top: 0px;
            text-align: right;
            border-top: 1px solid #e5e5e5;
        }

        .modal-header {
            min-height: 16.428571429px;
            padding: 6px 14px;
            border-bottom: 1px solid #e5e5e5;
        }

        blockquote p {
            font-size: 14px !important;
            font-weight: 300;
            line-height: 1.25;
        }
    </style>
    <form id="manupatraresult" enctype="multipart/form-data">
        <aside class="right-side">
            <section class="content-header">
                <!-- Content Header (Page header) -->
                <ol class="breadcrumb">
                    <li><a href="/@Session["firmcode"]/firm/dashboard"><i class="fa fa-dashboard"></i> Home</a></li>
                    <li class="active">Manupatra Search</li>
                </ol>
                <h1><i class="glyphicon glyphicon-search"></i> Manupatra  Search</h1>
            </section>
            <section class="content">
                <div id="page-wrapper">
                    <div class="container-fluid showcontent" id="showcontent">
                        <div class="box">
                            <div class="box-header" style="padding:5px;">
                                <ul class="btn_panel no-margin pull-right" style="padding:0;" id="sectiontemp">
                                    <li>
                                        <select class="form-control" name="state" id="maxRows" style="width:144px !important;display:none">
                                            <option value="5000" selected>Show All Rows</option>
                                            <option value="5" selected>5</option>
                                            <option value="10">10</option>
                                            <option value="20">20</option>
                                            <option value="50">50</option>
                                            <option value="70">70</option>
                                            <option value="100">100</option>
                                        </select>
                                    </li>
                                    <li>
                                        <input type="text" id="searchtxt" required class="form-control2 manusearchmobdesign" placeholder="Search text" style="float:left;">
                                        <button type="button" id="idss" class="btn btn-primary" style="padding:8px 12px 9px 12px; margin:0 0 0 2px; float:left;">
                                            <span class="glyphicon glyphicon-search"></span>
                                        </button>
                                    </li>
                                    <li>
                                        <select class="form-control select_court" id="court" name="court" style="width:100px;"></select>
                                    </li>
                                    <li>
                                        <ul class="btn_panel no-margin pull-right" style="padding:2px 5px 1px 5px; background:#e0dfdf; border:1px solid #cccccc;">
                                            <li style="padding:5px 0 0 0; font-weight:bold;">Search Criteria:</li>
                                            <li style="padding:2px 0 0 0;"><input type="radio" name="searchtype" value="searchfield" checked="checked">&nbsp;<label for="">Free Text Search</label></li>
                                            <li style="padding:2px 0 0 0;"><input type="radio" name="searchtype" value="title">&nbsp;<label for="">Title Search</label></li>
                                            <li style="padding:2px 0 0 0;"><input type="radio" name="searchtype" value="equicitation">&nbsp;<label for="">Citation</label></li>
                                        </ul>
                                    </li>
                                </ul>
                            </div>
                        </div><!-- /.box-header -->
                        <!-- /.panel-heading -->
                        <script>
                        </script>
                        <script src="~/CssJsfiles/js/pdfmake.min.js"></script>
                        <script src="~/CssJsfiles/js/html2canvas.min.js"></script>
                        <script type="text/javascript">
                            $("body").on("click", "#btnExport", function () {
                                html2canvas($('#example')[0], {
                                    onrendered: function (canvas) {
                                        var data = canvas.toDataURL();
                                        var docDefinition = {
                                            content: [{
                                                image: data,
                                                width: 500
                                            }]
                                        };
                                        pdfMake.createPdf(docDefinition).download("Table.pdf");
                                    }
                                });
                            });
                        </script>
                        <div id="updatePanel1" style="overflow-x:auto;">
                        </div>
                        <div><center><span id="datastatus"></span></center></div>
                        <div class="box-footer clearfix">
                            <ul class="pagination pagination-sm no-margin pull-right"></ul>
                        </div>
            </section>
        </aside>
        <!-- Modal -->
        <div id="myModal" class="modal fade" role="dialog">
            <div class="modal-dialog ">
                <!-- Modal content-->
                <div class="modal-content">
                    <div class="modal-header btn-primary">
                        <button type="button" class="close" data-dismiss="modal">&times;</button>
                        <h4 class="modal-title">
                            Manupatra
                        </h4>
                    </div>
                    <div class="modal-body">
                        <div style="min-height:480px; overflow-y:scroll;" id="doctxt"></div>
                    </div>
                </div>
            </div>
        </div>
    </form>
    <script src="~/PanelDesign/vendor/bootstrap/js/bootstrap.min.js"></script>
    @*<script src='https://cdnjs.cloudflare.com/ajax/libs/jquery/2.1.3/jquery.min.js'></script>
        <script src='https://cdnjs.cloudflare.com/ajax/libs/twitter-bootstrap/3.2.0/js/bootstrap.min.js'></script>*@
    <script>
        $(document).ready(function () {
            function openload() {
                $('#myOverlay').css("display", "block");
            }
            function closeload() {
                $('#myOverlay').css("display", "none");
            }
            $(document).on("click", "#idss", function () {
                var formData = new FormData();
                var strtable = '';
                var strheader = '';
                var stritem = '';
                var pno = 1;
                var searchtxt = $('#searchtxt').val().replace("“", "\"").replace("”", "\"").replace("�", "\"").replace("�", "\"");;
                var courttoc = $('#court').val();
                strtable = $('<table id="example" style="overflow-x:auto;" /><tr><th>').addClass('sortable dataTable table table-bordered table-striped');
                var searchtype = $('input[name="searchtype"]:checked').val();
                if (searchtxt != "") {
                    openload();
                    $.ajax(
                        {
                            type: "POST",
                            url: "/api/WorkFlowNewApi/ManupatraSubscription", // Controller/View
                            dataType: 'json',
                            headers: {
                                'qry': searchtxt, 'courttoc': courttoc, 'stype': searchtype, 'pno': pno
                            },
                            contentType: 'application/json;charset=utf-8',
                            processData: false,
                            success: function (response) {
                                if (response.Status == true) {
                                    var datas = JSON.stringify(response);
                                    strheader = $('<thead> <tr>< th >Your search result for <strong>court</strong> is as follows :</th ></tr ></thead>');
                                    strtable.append(strheader);
                                    stritem = "<tbody>";
                                    if (response.Data != null) {
                                        $.each(response.Data, function (i, val) {
                                            stritem += '<tr><td><p><a id="getdocument" data-internalid="' + val.internalid + '"   data-path="' + val.path + '"   data-collection="' + val.collection + '"> ' + val.title + '</a></p><br><p>' + val.body + '</p></td></tr>';
                                        });
                                        stritem += "</tbody>";
                                        strtable.append(stritem);
                                        $('#updatePanel1').html(strtable);
                                        getPaginationdefault('#example');
                                        closeload();
                                    }
                                    else {
                                        alert("No Result Found");
                                        $('#updatePanel1').html("");
                                        closeload();
                                        return false;
                                    }
                                }
                            },
                            failure: function (data) {
                                alert(data.responseText);
                            },
                            error: function (data) {
                                alert(data.responseText);
                            }
                        });
                    $('.pagination').empty();
                }
                else {
                    alert("Please enter text to search");
                    return false;
                }
            });
            openload();
            $.ajax(
                {
                    type: "GET",
                    url: "/api/WorkFlowNewApi/GetCourtList", // Controller/View
                    dataType: 'json',
                    contentType: 'application/json;charset=utf-8',
                    processData: false,
                    success: function (response) {
                        if (response.Status == true) {
                            var datas = JSON.stringify(response);
                            $.each(response.Data, function (i, val) {
                                var option = '<option value="' + val["CourtVal"] + '" > ' + val["Court"] + '</option>';
                                $("#court").append(option);
                            });
                            closeload();
                        }
                    },
                    failure: function (data) {
                        alert(data.responseText);
                    },
                    error: function (data) {
                        alert(data.responseText);
                    }
                });
            $(document).on("click", "#getdocument", function () {
                var inertid = $(this).attr("data-internalid");
                var path = $(this).attr("data-path");
                var collection = $(this).attr("data-collection");
                getdoc(inertid, path, collection);
            });
            function getdoc(filename, filepath, filecol) {
                filename = '?docid=' + filename;
                openload();
                $.ajax(
                    {
                        type: "GET",
                        url: "/api/WorkFlowNewApi/getDoc", // Controller/View
                        dataType: 'json',
                        headers: {
                            'filename': filename, 'filepath': filepath, 'filecol': filecol
                        },
                        contentType: 'application/json;charset=utf-8',
                        processData: false,
                        success: function (response) {
                            if (response.Status == true) {
                                $("#doctxt").html(response.Data);
                                $('#myModal').modal({ show: true });
                                closeload();
                            }
                        },
                        failure: function (data) {
                            alert(data.responseText);
                        },
                        error: function (data) {
                            alert(data.responseText);
                        }
                    });
            }
        });
    </script>
    <script>
        /*					PAGINATION
        - on change max rows select options fade out all rows gt option value mx = 5
        - append pagination list as per numbers of rows / max rows option (20row/5= 4pages )
        - each pagination li on click -> fade out all tr gt max rows * li num and (5*pagenum 2 = 10 rows)
        - fade out all tr lt max rows * li num - max rows ((5*pagenum 2 = 10) - 5)
        - fade in all tr between (maxRows*PageNum) and (maxRows*pageNum)- MaxRows
        */
        getPagination('#example');
        function getPagination(table) {
            $('#maxRows').on('change', function () {
                $('.pagination').html('');						// reset pagination
                var trnum = 0;									// reset tr counter
                var maxRows = parseInt($(this).val());
                var totalRows = $(table + ' tbody tr ').length;
                $('#example tbody tr').each(function () {			// each TR in  table and not the header
                    trnum++;
                    if (trnum > maxRows) {						// if tr number gt maxRows
                        $(this).hide();							// fade it out
                    } if (trnum <= maxRows) { $(this).show(); }// else fade in Important in case if it ..
                });											//  was fade out to fade it in
                if (totalRows > maxRows) {						// if tr total rows gt max rows option
                    var pagenum = Math.ceil(totalRows / maxRows);
                    // alert(pagenum);// ceil total(rows/maxrows) to get ..
                    //	numbers of pages
                    for (var i = 1; i <= pagenum;) {			// for each page append pagination li
                        $('.pagination').append('<li data-page="' + i + '">\
                                                                                                                                                                                                                                                                                                                                                                                                                                                                              <span>'+ i++ + '<span class="sr-only">(current)</span></span>\
                                                                                                                                                                                                                                                                                                                                                                                                                                                                            </li>').show();
                    }											// end for i
                } 												// end if row count > max rows
                $('.pagination li:first-child').addClass('active'); // add active class to the first li
                $('.pagination li').on('click', function () {		// on click each page
                    var pageNum = $(this).attr('data-page');
                    // alert(pageNum);// get it's number
                    var trIndex = 0;							// reset tr counter
                    $('.pagination li').removeClass('active');	// remove active class from all li
                    $(this).addClass('active');					// add active class to the clicked
                    $('#example tbody tr').each(function () {		// each tr in table not the header
                        trIndex++;
                        // if tr index gt maxRows*pageNum or lt maxRows*pageNum-maxRows fade if out
                        if (trIndex > (maxRows * pageNum) || trIndex <= ((maxRows * pageNum) - maxRows)) {
                            $(this).hide();
                        } else { $(this).show(); } 				//else fade in
                    }); 										// end of for each tr in table
                });										// end of on click pagination list
            });
            // end of on select change
            // END OF PAGINATION
        }
        function getPaginationdefault(table) {
            $('.pagination').html('');						// reset pagination
            var trnum = 0;									// reset tr counter
            var maxRows = 5;
            // alert(maxRows);// get Max Rows from select option
            var totalRows = $(table + ' tbody tr ').length;
            // alert(totalRows);// numbers of rows
            $('#example tbody tr').each(function () {			// each TR in  table and not the header
                trnum++;
                if (trnum > maxRows) {						// if tr number gt maxRows
                    $(this).hide();							// fade it out
                } if (trnum <= maxRows) { $(this).show(); }// else fade in Important in case if it ..
            });											//  was fade out to fade it in
            if (totalRows > maxRows) {						// if tr total rows gt max rows option
                var pagenum = Math.ceil(totalRows / maxRows);
                // alert(pagenum);// ceil total(rows/maxrows) to get ..
                //	numbers of pages
                for (var i = 1; i <= pagenum;) {			// for each page append pagination li
                    $('.pagination').append('<li data-page="' + i + '">\
                                                                                                                                                                                                                                                                                                                                                                                                                                                                              <span>'+ i++ + '<span class="sr-only">(current)</span></span>\
                                                                                                                                                                                                                                                                                                                                                                                                                                                                            </li>').show();
                }											// end for i
            } 												// end if row count > max rows
            $('.pagination li:first-child').addClass('active'); // add active class to the first li
            $('.pagination li').on('click', function () {		// on click each page
                var pageNum = $(this).attr('data-page');
                var trIndex = 0;							// reset tr counter
                $('.pagination li').removeClass('active');	// remove active class from all li
                $(this).addClass('active');					// add active class to the clicked
                $('#example tbody tr').each(function () {		// each tr in table not the header
                    trIndex++;
                    // if tr index gt maxRows*pageNum or lt maxRows*pageNum-maxRows fade if out
                    if (trIndex > (maxRows * pageNum) || trIndex <= ((maxRows * pageNum) - maxRows)) {
                        $(this).hide();
                    } else { $(this).show(); } 				//else fade in
                }); 										// end of for each tr in table
            });										// end of on click pagination list
            // end of on select change
            // END OF PAGINATION
        }
    </script>
    <div id="myOverlay" class="overlay">
        <div class="overlay-content">
            <div class="spinner2">
                <div><img src="~/newassets/images/MyKase_Loader.gif" height="59" width="150"></div>
            </div>
        </div>
    </div>
    <style>
        .overlay {
            height: 100%;
            width: 100%;
            display: none;
            position: fixed;
            z-index: 1;
            top: 0;
            left: 0;
            background-color: rgb(0,0,0);
            background-color: rgba(0,0,0, 0.4);
        }

        .overlay-content {
            position: relative;
            top: 46%;
            width: 80%;
            text-align: center;
            margin-top: 30px;
            margin: auto;
            color: white;
        }

        .overlay .closebtn {
            position: absolute;
            top: 20px;
            right: 45px;
            font-size: 60px;
            cursor: pointer;
            color: white;
        }

            .overlay .closebtn:hover {
                color: #ccc;
            }
    </style>
}